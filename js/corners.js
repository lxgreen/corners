var Corners = Corners || {
    // define board that exposes state object (read-only), functions : init(), setChecker(point, checker)
    board : (function board(){
        var state =
            init = function init(){
        },


        return {

        };
    })(),
    }
};
